<mvc:View xmlns:core="sap.ui.core" xmlns:l="sap.ui.layout" xmlns:f="sap.ui.layout.form"
          xmlns:html="http://www.w3.org/1999/xhtml" xmlns:mvc="sap.ui.core.mvc" xmlns:t="sap.ui.table" xmlns="sap.m"
          controllerName="sapui5agendar.sco00mantenedorcliente_html.controller.Create">
    <App class="sapUiSizeCompact" id="app">
        <Page showHeader="false">
            <subHeader>
                <Toolbar id="filters">
                    <Button icon="sap-icon://nav-back" text="Atrás" press="handleBack"/>
                </Toolbar>
            </subHeader>
            <content>
                <core:Fragment type="XML" fragmentName="sapui5agendar.sco00mantenedorcliente_html.view.EditForm" />
            </content>
            <footer>
                <Toolbar>
                    <Button
                            id="errorPopover"
                            icon="sap-icon://message-popup"
                            visible="{= ${message>/}.length > 0}"
                            text="{= ${message>/}.length}"
                            type="Emphasized"
                            press="handleMessagePopoverPress"/>
                    <ToolbarSpacer/>
                    <Button icon="sap-icon://save" text="Guardar" press="handleSave" enabled="{/EditEnabled}"/>
                </Toolbar>
            </footer>
        </Page>
    </App>
</mvc:View>