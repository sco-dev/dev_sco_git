<core:FragmentDefinition xmlns:core="sap.ui.core"
                         xmlns:t="sap.ui.table"
                         xmlns="sap.m">
    <t:Table rows="{Notificables}" selectedIndex="{i_Notificable}" selectionMode="Single" selectionBehavior="RowOnly" visibleRowCount="10" noData="No se encontraron operaciones para notificar" id="tableNotificar">
        <t:extension>
            <Toolbar>
                <Button text="Volver" icon="sap-icon://nav-back" press="handleOperacionesVolver"/>
                <Button text="Notificar" icon="sap-icon://bell" press="handleOperacionesNotificar" enabled="{=${i_Notificable} >= 0}" visible="{=${Notificables}.length > 0}"/>
            </Toolbar>
        </t:extension>
        <t:columns>
            <t:Column autoResizable="true">
                <Label text="Notificar"/>
                <t:template>
                    <CheckBox selected="{NOTIFICAR}" select="handleNotificaCheckChange"/>
                </t:template>
            </t:Column>
            <t:Column autoResizable="true">
                <Label text="Operación"/>
                <t:template>
                    <Text text="{OPERACION}"/>
                </t:template>
            </t:Column>
            <t:Column autoResizable="true">
                <Label text="Sub. Oper."/>
                <t:template>
                    <Text text="{SUB_OPERACION}"/>
                </t:template>
            </t:Column>
            <t:Column autoResizable="true">
                <Label text="Puesto Trabajo"/>
                <t:template>
                    <Text text="{PUESTO_TRABAJO}"/>
                </t:template>
            </t:Column>
            <t:Column autoResizable="true">
                <Label text="Centro"/>
                <t:template>
                    <Text text="{CENTRO}"/>
                </t:template>
            </t:Column>
            <t:Column autoResizable="true">
                <Label text="Clave Control"/>
                <t:template>
                    <Text text="{CLAVE_CONTROL}"/>
                </t:template>
            </t:Column>
            <t:Column autoResizable="true">
                <Label text="Clave Modelo"/>
                <t:template>
                    <Text text="{CLAVE_MODELO}"/>
                </t:template>
            </t:Column>
            <t:Column autoResizable="true">
                <Label text="Texto Operación"/>
                <t:template>
                    <Text text="{TXT_OPERACION}"/>
                </t:template>
            </t:Column>
            <t:Column autoResizable="true">
                <Label text="Trabajo"/>
                <t:template>
                    <Text text="{TRABAJO}"/>
                </t:template>
            </t:Column>
            <t:Column autoResizable="true">
                <Label text="Unidad"/>
                <t:template>
                    <Text text="{UNIDAD}"/>
                </t:template>
            </t:Column>
            <t:Column autoResizable="true">
                <Label text="Horas Notificadas"/>
                <t:template>
                    <Text text="{HORAS}"/>
                </t:template>
            </t:Column>
            <t:Column autoResizable="true">
                <Label text="% Notificación"/>
                <t:template>
                    <Text text="{PORC_NOTIFICACION}%"/>
                </t:template>
            </t:Column>
            <t:Column autoResizable="true">
                <Label text="Clase Actividad"/>
                <t:template>
                    <Text text="{CLASE_ACTIVIDAD}"/>
                </t:template>
            </t:Column>
            <t:Column autoResizable="false" width="10rem">
                <Label text="Capacidad Individual"/>
                <t:template>
                    <CheckBox selected="{CAPACIDAD_INDIVIDUAL}" enabled="false"/>
                </t:template>
            </t:Column>
        </t:columns>
        <t:footer>
            <Toolbar>
                <Text text="Total: {= ${Notificables}.length }"/>
            </Toolbar>
        </t:footer>
    </t:Table>
</core:FragmentDefinition>